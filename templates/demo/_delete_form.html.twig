<form method="post" action="{{ path('demo_delete', {'id': demo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demo.id) }}">
    <button class="btn">Delete</button>
</form>
